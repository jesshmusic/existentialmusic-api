<%= form_with(model: dev_resume) do |form| %>
  <% if dev_resume.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(dev_resume.errors.count, "error") %> prohibited this dev_resume from being saved:</h2>

      <ul>
        <% dev_resume.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
