<%= form_with(model: dev_home) do |form| %>
  <% if dev_home.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(dev_home.errors.count, "error") %> prohibited this dev_home from being saved:</h2>

      <ul>
        <% dev_home.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
